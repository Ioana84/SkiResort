import { GoogleOAuthProvider } from '@react-oauth/google';

export default class GoogleIdentityService {
    
    private client_id: string = process.env.REACT_APP_GOOGLE_CLIENT_ID || ""

    constructor () {

    }

}